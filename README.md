# releases-bin

### ENVS

All envs are optional

* VERSION   the version to download, default to 'latest'
* CMD       the CMD to run, default to blank
* EXPOSE    the EXPOSE port


```
docker run --rm -it releases-bin username/repo binary_name params
```
